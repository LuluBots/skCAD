# STL regression fixtures

Place STL files that reproduce voxelization or pattern-conversion failures in
this directory. Use short, stable filenames and record the corresponding
`Author compatibility` and comparative diagnostic output in the issue or test
that uses the fixture.

Large or private models do not need to be committed. They can remain local in
this directory while being used for reproducible browser tests.
